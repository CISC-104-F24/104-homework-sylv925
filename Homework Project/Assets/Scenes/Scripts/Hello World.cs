using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello, World!");
        Debug.LogWarning("Hello, I am a warning!");
        Debug.LogError("I am an Error");
        //AuthorSylv
        /*
        *I hope to learn basic coding!
        *How long should we expect to work on projects outside of class each week?
        *Until Then is a visual novel set in the Philipines with themes of grief and growing up.
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
